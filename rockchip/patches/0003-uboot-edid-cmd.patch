--- a/common/edid.c	2025-08-15 15:04:24.092517855 +0800
+++ b/common/edid.c	2025-08-15 15:04:48.696591900 +0800
@@ -3579,7 +3579,7 @@
 {
 	const u8 *cea = drm_find_cea_extension(edid);
 	const u8 *db, *hdmi = NULL, *video = NULL;
-	u8 dbl, hdmi_len, video_len = 0;
+	u8 dbl, hdmi_len = 0, video_len = 0;
 	int modes = 0;

 	if (cea && cea_revision(cea) >= 3) {
--- a/common/command.c	2024-12-02 11:01:52.000000000 +0800
+++ b/common/command.c	2025-08-15 15:20:31.806430712 +0800
@@ -501,8 +501,8 @@
 	return result;
 }
 
-enum command_ret_t cmd_process(int flag, int argc, char * const argv[],
-			       int *repeatable, ulong *ticks)
+int cmd_process(int flag, int argc, char * const argv[],
+		int *repeatable, ulong *ticks)
 {
 	enum command_ret_t rc = CMD_RET_SUCCESS;
 	cmd_tbl_t *cmdtp;
